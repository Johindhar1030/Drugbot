services:
  - type: web
    name: drugbot
    env: python
    rootDir: drugbot
    buildCommand: pip install --upgrade pip && pip install --prefer-binary -r requirements.txt
    startCommand: bash -lc 'cd /opt/render/project/src/drugbot && PYTHONPATH=. python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT'
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PYTHONPATH
        value: .
